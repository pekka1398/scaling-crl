# RL hyperparameters — shared across all experiments
entropy_param: 0.5
gamma: 0.99
logsumexp_penalty_coeff: 0.1
disable_entropy: 0
use_relu: 0
num_episodes_per_env: 1
training_steps_multiplier: 1
use_all_batches: 0
num_sgd_batches_per_training_step: 800
